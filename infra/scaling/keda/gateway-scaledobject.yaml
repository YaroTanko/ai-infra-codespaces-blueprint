apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: agent-gateway-scale
  namespace: gateway-system
spec:
  scaleTargetRef:
    name: agent-gateway
  minReplicaCount: 2
  maxReplicaCount: 10
  triggers:
    - type: cpu
      metadata:
        type: Utilization
        value: "70"
