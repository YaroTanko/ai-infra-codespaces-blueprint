# Evaluation Thresholds

- Gateway P95 latency: <= 500 ms.
- Agent task success rate: >= 98%.
- Tool-call failure rate: <= 1%.
- Terminal Bench score: >= configured threshold.
- Cost per task: must not regress > 10% from baseline.
