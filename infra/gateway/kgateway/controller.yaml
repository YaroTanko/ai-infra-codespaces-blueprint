apiVersion: apps/v1
kind: Deployment
metadata:
  name: kgateway-controller
  namespace: gateway-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kgateway-controller
  template:
    metadata:
      labels:
        app: kgateway-controller
    spec:
      containers:
        - name: controller
          image: ghcr.io/example/kgateway-controller:v0.1.0
          ports:
            - containerPort: 9443
