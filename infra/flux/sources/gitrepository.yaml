apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: ai-infra-config
  namespace: flux-system
spec:
  interval: 1m
  ref:
    branch: main
  url: https://github.com/example/ai-infra-codespaces-blueprint
