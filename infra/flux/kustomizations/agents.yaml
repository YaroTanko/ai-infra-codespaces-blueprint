apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: agents
  namespace: flux-system
spec:
  interval: 5m
  path: ./infra/agents
  prune: true
  sourceRef:
    kind: GitRepository
    name: ai-infra-config
