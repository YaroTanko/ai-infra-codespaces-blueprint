apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: security
  namespace: flux-system
spec:
  interval: 5m
  path: ./infra/security
  prune: true
  sourceRef:
    kind: GitRepository
    name: ai-infra-config
