{
  "name": "ai-infra-codespaces-blueprint",
  "version": "0.1.0",
  "private": true,
  "description": "AI Reliability Engineering Infrastructure Blueprint for Codespaces + Kubernetes",
  "scripts": {
    "lint": "markdownlint '**/*.md' --ignore node_modules",
    "lint:fix": "markdownlint '**/*.md' --ignore node_modules --fix",
    "test": "npm run lint"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/YaroTanko/ai-infra-codespaces-blueprint.git"
  },
  "license": "ISC",
  "devDependencies": {
    "markdownlint-cli": "^0.48.0"
  }
}
